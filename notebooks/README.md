We created a simple function. We did this by creating the function subtract(a, b) in Python. After this, you can recall this function and subtract the two numbers by adding these in the brackets. We tested the function with some sets of numbers; small numbers, big numbers, negative numbers, fractions, complex numbers. For all off these examples, the output was correct. For substracting strings or one string and one number the function doesn't work. 
